/*const mongoose = require('mongoose');

//Collection names always plural => end with s

const foodTruckSchema = new mongoose.Schema({
  name: String,
  ratings: [Number],
  ratingAvg: Number,
  restaurant: String,
  available: Boolean,
});

var foodTruckModel = mongoose.model('foodTrucks', foodTruckSchema); //collection
module.exports = foodTruckModel;*/
